{
  "name": "network-overrides",
  "version": "0.2.2",
  "description": "CLI and backend for the Network Overrides browser extension",
  "bin": {
    "network-overrides": "./cli.js"
  },
  "main": "./cli.js",
  "files": ["backend/*", "cli.js", "commands.js", "constants.js"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Miguel Silva",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/miguel-silva/network-overrides.git",
    "directory": "core"
  },
  "bugs": "https://github.com/miguel-silva/network-overrides/issues",
  "dependencies": {
    "fastify": "^3.14.0",
    "fastify-websocket": "^3.1.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
